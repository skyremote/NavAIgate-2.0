---
phase: 01-foundation
plan: 01
type: execute
---

<objective>
Set up Astro project with proper structure and extract design tokens from bluplai reference.

Purpose: Establish the technical foundation that all subsequent phases build upon, ensuring consistency with bluplai's visual identity from the start.
Output: Working Astro project with layouts, CSS animations, and Tailwind configuration ready for component development.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

**Reference codebase (source for patterns):**
@/Users/Sky/Documents/GitHub/bluplai/package.json
@/Users/Sky/Documents/GitHub/bluplai/astro.config.mjs
@/Users/Sky/Documents/GitHub/bluplai/tailwind.config.js
@/Users/Sky/Documents/GitHub/bluplai/src/index.css
@/Users/Sky/Documents/GitHub/bluplai/src/animations.css
@/Users/Sky/Documents/GitHub/bluplai/src/layouts/BaseLayout.astro

**Key design patterns from bluplai:**
- Astro 5.x with React integration, Tailwind, sitemap
- Poppins font via Google Fonts
- Dark theme (bg-gray-900, text-white)
- Custom CSS animations: float, gradientShift, slideIn variants, glass morphism, shimmer effects
- Framer Motion and GSAP for advanced animations
- Lucide React for icons
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Astro project with core dependencies</name>
  <files>package.json, astro.config.mjs, tsconfig.json, postcss.config.js, .gitignore</files>
  <action>
    Create new Astro project in the NavAIgate 2.0 directory. Install same core dependencies as bluplai:
    - astro ^5.x, @astrojs/react, @astrojs/tailwind, @astrojs/sitemap
    - react, react-dom (^18.x)
    - framer-motion, gsap (for animations)
    - lucide-react (for icons)
    - tailwindcss, autoprefixer, postcss
    - @tailwindcss/typography
    - typescript

    Configure astro.config.mjs with:
    - site: 'https://navaigate.dev'
    - output: 'static'
    - integrations: react(), tailwind(), sitemap()
    - vite.optimizeDeps.exclude: ['lucide-react']

    Do NOT use `npm create astro` interactively - write files directly to avoid prompts.
    Use the exact versions from bluplai's package.json where possible for consistency.
  </action>
  <verify>npm install succeeds, npm run dev starts without errors</verify>
  <done>package.json has all dependencies, astro.config.mjs configured, npm install completes without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create project structure and base layout</name>
  <files>src/layouts/BaseLayout.astro, src/pages/index.astro, src/components/.gitkeep, public/.gitkeep</files>
  <action>
    Create directory structure:
    - src/layouts/ (Astro layouts)
    - src/pages/ (Astro pages)
    - src/components/ (React/Astro components)
    - src/assets/ (images, fonts)
    - public/ (static assets)

    Create BaseLayout.astro adapted from bluplai with:
    - Props interface: title, description, image, canonicalURL
    - SEO meta tags (title, description, OG, Twitter)
    - Google Fonts preconnect for Poppins
    - Body class: min-h-screen bg-gray-900 text-white overflow-x-hidden font-poppins
    - Update site title pattern to "X | NavAIgate" and default description for NavAIgate

    Create minimal index.astro that imports BaseLayout with placeholder content.
  </action>
  <verify>npm run dev shows page with dark background, Poppins font loads, no console errors</verify>
  <done>Directory structure exists, BaseLayout.astro works, index.astro renders with correct styling</done>
</task>

<task type="auto">
  <name>Task 3: Configure design tokens and animations</name>
  <files>tailwind.config.js, src/index.css, src/animations.css</files>
  <action>
    Create tailwind.config.js with:
    - content paths for astro, tsx, ts files
    - Extended fontFamily for Poppins with fallbacks
    - @tailwindcss/typography plugin

    Create src/index.css with:
    - Tailwind directives (@tailwind base, components, utilities)
    - Global body font-poppins application
    - 3D transform utilities (preserve-3d, perspective-1000)

    Copy src/animations.css from bluplai containing:
    - Float animations (float, floatReverse)
    - Gradient shift animation (animated-gradient)
    - Pulse animation (pulse-slow)
    - Slide-in animations (slideInLeft, slideInRight, slideInUp)
    - Hover lift effect
    - Glass morphism (.glass)
    - Stagger animation classes
    - Counter animation
    - Button shimmer effect
    - Flow lines background animation

    Import both CSS files in BaseLayout.astro.
  </action>
  <verify>npm run build succeeds, CSS classes available, float animation works when applied to test element</verify>
  <done>Tailwind configured, all animation classes from bluplai available, build succeeds</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run dev` starts development server without errors
- [ ] `npm run build` completes successfully
- [ ] Homepage renders with dark theme (bg-gray-900)
- [ ] Poppins font loads from Google Fonts
- [ ] Animation classes (float, glass, etc.) work when applied
- [ ] No TypeScript errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Project structure matches bluplai patterns
- Design tokens extracted and functional
- Ready for Phase 2: Design System component development
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` following the summary template.
</output>
