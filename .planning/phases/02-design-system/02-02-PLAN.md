---
phase: 02-design-system
plan: 02
type: execute
---

<objective>
Create typography effect components for dynamic text animations.

Purpose: Port bluplai's signature text effects (ShinyText, RotatingText, GradientText) to enable eye-catching headlines and labels.
Output: Three React components with CSS animations and Framer Motion integration.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./02-02-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-design-system/02-01-SUMMARY.md

**Reference files from bluplai:**
- /Users/Sky/Documents/GitHub/bluplai/src/components/ShinyText.tsx
- /Users/Sky/Documents/GitHub/bluplai/src/components/ShinyText.css
- /Users/Sky/Documents/GitHub/bluplai/src/components/RotatingText.tsx
- /Users/Sky/Documents/GitHub/bluplai/src/components/RotatingText.css

**Tech stack available:**
- React 18.3.1, Framer Motion 12.18.1, Tailwind 3.4.1

**Established patterns:**
- CSS animations in src/animations.css
- Dark theme with light text
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ShinyText component with sweep animation</name>
  <files>src/components/ShinyText.tsx, src/components/ShinyText.css</files>
  <action>
Port ShinyText from bluplai:

ShinyText.tsx:
- Props: text (string), speed (number, default 5), disabled (boolean), className (string)
- Uses CSS custom property --shine-duration for animation speed
- Applies .shiny-text class with conditional .disabled

ShinyText.css:
- Linear gradient from gray to white to gray (creates shine sweep)
- background-size: 200% auto for animation range
- background-clip: text with -webkit-text-fill-color: transparent
- @keyframes shine animates background-position from -200% to 200%
- .disabled class removes animation

Import CSS in the component file.
  </action>
  <verify>npx astro check passes</verify>
  <done>ShinyText component renders animated shimmering text effect</done>
</task>

<task type="auto">
  <name>Task 2: Create RotatingText component with Framer Motion</name>
  <files>src/components/RotatingText.tsx, src/components/RotatingText.css</files>
  <action>
Port RotatingText from bluplai (simplified version):

RotatingText.tsx:
- Props: texts (string[]), rotationInterval (number, default 2500), transition (Framer Transition), mainClassName, splitLevelClassName
- Uses Framer Motion's AnimatePresence for enter/exit animations
- Splits text into characters for staggered animation
- Default animation: y slides from 100% to 0 to -120%
- Uses useEffect interval to cycle through texts array

RotatingText.css:
- .text-rotate: inline-flex, flex-wrap, position relative
- .text-rotate-word: inline-flex
- .text-rotate-element: inline-block
- .text-rotate-space: white-space pre
- Screen reader accessible text via sr-only pattern

Use forwardRef with imperative handle for next/previous/jumpTo controls.
Simpler than bluplai version - focus on core rotation effect.
  </action>
  <verify>npx astro check passes</verify>
  <done>RotatingText cycles through text array with smooth Framer Motion transitions</done>
</task>

<task type="auto">
  <name>Task 3: Create GradientText utility component</name>
  <files>src/components/GradientText.tsx</files>
  <action>
Create simple GradientText wrapper component:

Props:
- children: ReactNode
- gradient: 'default' | 'blue-purple' | 'purple-teal' | 'rainbow' (predefined gradients)
- className: string (additional classes)
- as: 'span' | 'h1' | 'h2' | 'h3' | 'p' (element type, default 'span')

Gradient presets:
- default: from-blue-400 via-purple-400 to-teal-400
- blue-purple: from-blue-400 to-purple-400
- purple-teal: from-purple-400 to-teal-400
- rainbow: from-blue-400 via-purple-400 via-pink-400 to-orange-400

Apply: bg-gradient-to-r bg-clip-text text-transparent
Allow custom gradient via className override.
  </action>
  <verify>npx astro check passes</verify>
  <done>GradientText renders text with gradient color fill</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx astro check` passes without errors
- [ ] ShinyText animates with sweep effect
- [ ] RotatingText cycles through provided texts
- [ ] GradientText applies gradient to text content
- [ ] All CSS files properly imported
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript errors
- Typography effects match bluplai visual style
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system/02-02-SUMMARY.md`
</output>
