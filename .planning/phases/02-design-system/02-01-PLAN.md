---
phase: 02-design-system
plan: 01
type: execute
---

<objective>
Create core UI primitive components matching bluplai aesthetic.

Purpose: Establish reusable Button, Badge, and Card components that form the foundation for all UI sections.
Output: Three React components with proper TypeScript types, following bluplai's gradient, glass, and animation patterns.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./02-01-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

**Reference files from bluplai:**
- /Users/Sky/Documents/GitHub/bluplai/src/components/CTA.tsx (button patterns)
- /Users/Sky/Documents/GitHub/bluplai/src/components/Hero.tsx (badge/glass patterns)

**Existing animations (from Phase 1):**
- src/animations.css: glass, hover-lift, btn-shimmer, animated-gradient

**Tech stack available:**
- React 18.3.1, Tailwind 3.4.1, Framer Motion 12.18.1, Lucide React 0.344.0

**Established patterns:**
- Dark theme (bg-gray-900) as default
- Poppins typography
- Glass morphism via .glass class
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Button component with variants</name>
  <files>src/components/Button.tsx</files>
  <action>
Create a React Button component with TypeScript props:
- variant: 'primary' (gradient blue-to-purple) | 'secondary' (ghost/text style) | 'outline' (border only)
- size: 'sm' | 'md' | 'lg'
- shimmer: boolean (adds btn-shimmer class for hover effect)
- icon: ReactNode (optional trailing icon)
- asChild: boolean (for link usage with Astro)

Primary variant: bg-gradient-to-r from-blue-500 to-purple-600, hover:from-blue-600 hover:to-purple-700
Secondary variant: text-gray-300 hover:text-white with optional play button circle
Include group hover effects for icon animation (translate-x-1)
Use forwardRef for proper ref handling.
  </action>
  <verify>TypeScript compiles without errors: npx astro check</verify>
  <done>Button.tsx exists with primary/secondary/outline variants, shimmer effect, and proper TypeScript types</done>
</task>

<task type="auto">
  <name>Task 2: Create Badge component with glass morphism</name>
  <files>src/components/Badge.tsx</files>
  <action>
Create a React Badge/Pill component:
- variant: 'glass' (default, uses .glass class) | 'solid' (bg-gray-800) | 'gradient' (animated-gradient)
- size: 'sm' | 'md'
- icon: ReactNode (optional leading icon, typically Sparkles from lucide-react)
- children: ReactNode

Glass variant uses: backdrop-filter blur, rgba background, subtle border
Structure: rounded-full px-4 py-2 flex items-center space-x-2
Support both span and div rendering via 'as' prop.
  </action>
  <verify>TypeScript compiles: npx astro check</verify>
  <done>Badge.tsx exists with glass/solid/gradient variants and icon support</done>
</task>

<task type="auto">
  <name>Task 3: Create Card component with hover effects</name>
  <files>src/components/Card.tsx</files>
  <action>
Create a React Card component:
- variant: 'glass' (default) | 'solid' | 'outline'
- hover: boolean (adds hover-lift class for translateY and shadow on hover)
- padding: 'none' | 'sm' | 'md' | 'lg'
- className: string (for additional styling)
- children: ReactNode

Glass variant: .glass class (backdrop-blur, rgba background, border)
Solid variant: bg-gray-800/50 with border-gray-700
Include rounded-2xl by default.
Support onClick for interactive cards.
  </action>
  <verify>TypeScript compiles: npx astro check</verify>
  <done>Card.tsx exists with glass/solid/outline variants and hover-lift animation</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx astro check` passes without errors
- [ ] All three components export properly from their files
- [ ] TypeScript types are complete (no 'any' types)
- [ ] Components use existing animation classes from animations.css
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript errors
- Components follow bluplai patterns (gradient buttons, glass badges, hover cards)
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system/02-01-SUMMARY.md`
</output>
